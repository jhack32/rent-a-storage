<%= form_for :comment, url: '/users/' + @user.id.to_s + '/comments' do |t| %>
  <%= t.label :rating %>
  <%= t.select :rating, [1,2,3,4,5] %>
  <br>
  <%= t.label :text %>
  <%= t.text_area :text %>

  <% t.submit %>

<% end %>
